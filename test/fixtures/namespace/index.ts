export namespace a {
  export const answer = 42
}
export namespace a {
  export const pi = Math.PI
}
export namespace f {
  export const f = (x: number, y: number) => x + y
}
export namespace gg {
  export const b = { a, f }
}
